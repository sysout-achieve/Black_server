language : java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

before_deploy:
  - zip -r was-service *
  - mkdir -p deploy
  - mv was-service.zip deploy/was-service.zip

#deploy:
#  - provider: s3
#    access_key_id: $AWS_ACCESS_KEY
#    secret_access_key: $AWS_SECRET_KEY
#    bucket: was-service-build
#    region: ap-northeast-2
#    cleanup: true
#    acl: private
#    local_dir: deploy

#CI 실행 완료 시 메일로 알람

notifications:
  email:
    recipients:
      - 0408gun@gmail.com
